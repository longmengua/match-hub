syntax = "proto3";

package common;

option go_package = "match/proto;proto";

// 通用回應格式
message CommonResponse {
  int32  code = 1;                   // 狀態碼
  string message = 2;                // 描述訊息
}